# Synthetic examples

These files are invented test data. They do not report real research.

The [`synthetic-submission`](synthetic-submission) directory demonstrates one possible collection of a PDF, source text, code, data, and results. AIDaR does not require this structure or these file types.

The source contains fake identity signals. The pilot uses them to verify that supported terms are replaced only in the temporary submission snapshot. The source files remain unchanged.
